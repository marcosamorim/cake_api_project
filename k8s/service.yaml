apiVersion: v1
kind: Service
metadata:
  name: cake-api
  labels:
    app: cake-api
spec:
  type: ClusterIP
  selector:
    app: cake-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
